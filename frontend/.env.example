# Copy this to .env.local for local development
# For production (Netlify), set VITE_API_URL in the Netlify dashboard

# Your deployed backend URL (Render/Railway/etc.)
# Leave empty to use the local dev proxy (localhost:8000)
VITE_API_URL=
